{
  "repositories": [
    {
      "type": "vcs",
      "url": "https://github.com/PHPBrickLayer/structure"
    }
  ],
  "scripts": {
    "post-autoload-dump": [
      "@php bob project:create --new-project"
    ],

    "post-update-cmd": [
      "@php bob project:create --composer-update"
    ],

    "post-root-package-install": [
      "@php bob project:create --update-lay-structure"
    ]
  },

  "require": {
    "php": ">8.1",
    "bricklayer/structure": "dev-dev"
  },
  "require-dev": {
    "phpunit/phpunit": "^11.0-dev"
  }
}