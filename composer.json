{
  "scripts": {
    "post-autoload-dump": [
      "@php bob project:create --composer-install-on-directory"
    ],

    "post-update-cmd": [
      "@php bob project:create --composer-update"
    ],

    "post-root-package-install": [
      "@php bob project:create --update-lay-structure-on-existing-project"
    ],
    "post-create-project-cmd": [
      "@php bob project:create --fresh-new-project"
    ]
  },

  "require": {
    "php": ">8.1",
    "bricklayer/structure": "^0.2-alpha"
  },
  "require-dev": {
    "phpunit/phpunit": "^11.0-dev"
  }
}